// This is a skeleton starter React component generated by Plasmic.
// This file is owned by you, feel free to edit as you see fit.
import { isDoctorUser } from '@/common/hooks/useDoctorHomeRedirect';
import { useHamdastAppListParams, usePrefetchHamdastLauncherAppLists } from '@/modules/hamdast/hooks/usePrefetchHamdastLauncherAppLists';
import { useUserInfoStore } from '@/modules/login/store/userInfo';
import * as React from 'react';
import {
  PlasmicLauncherBlocksFeaturedApps,
  DefaultLauncherBlocksFeaturedAppsProps
} from "./plasmic/launcher/PlasmicLauncherBlocksFeaturedApps";
import { HTMLElementRefOf } from "@plasmicapp/react-web";

export interface LauncherBlocksFeaturedAppsProps
  extends DefaultLauncherBlocksFeaturedAppsProps {}

function LauncherBlocksFeaturedApps_(
  props: LauncherBlocksFeaturedAppsProps,
  ref: HTMLElementRefOf<"div">
) {
  const user = useUserInfoStore(state => state.info);
  const isDoctor = isDoctorUser(user);
  const hamdastAppListParams = useHamdastAppListParams();

  usePrefetchHamdastLauncherAppLists();

  return (
    <PlasmicLauncherBlocksFeaturedApps
      key={`featured-apps-${user?.id ?? 'guest'}-${isDoctor ? 'doctor' : 'patient'}-${Object.keys(hamdastAppListParams).length}`}
      root={{ ref }}
      {...props}
      apiRequest3={{
        params: hamdastAppListParams as Record<string, string | string[]>,
      }}
      apiRequest2={{
        params: hamdastAppListParams as Record<string, string | string[]>,
      }}
    />
  );
}

const LauncherBlocksFeaturedApps = React.forwardRef(
  LauncherBlocksFeaturedApps_
);
export default LauncherBlocksFeaturedApps;
