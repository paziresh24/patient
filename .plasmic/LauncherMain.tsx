// This is a skeleton starter React component generated by Plasmic.
// This file is owned by you, feel free to edit as you see fit.
import { isDoctorUser } from '@/common/hooks/useDoctorHomeRedirect';
import { usePrefetchHamdastLauncherAppLists } from '@/modules/hamdast/hooks/usePrefetchHamdastLauncherAppLists';
import { useUserInfoStore } from '@/modules/login/store/userInfo';
import * as React from 'react';
import {
  PlasmicLauncherMain,
  DefaultLauncherMainProps
} from "./plasmic/launcher/PlasmicLauncherMain";
import { HTMLElementRefOf } from "@plasmicapp/react-web";

export interface LauncherMainProps extends DefaultLauncherMainProps {}

function LauncherMain_(props: LauncherMainProps, ref: HTMLElementRefOf<"div">) {
  const user = useUserInfoStore(state => state.info);
  const isDoctor = isDoctorUser(user);

  usePrefetchHamdastLauncherAppLists();

  return (
    <PlasmicLauncherMain
      key={`launcher-main-${user?.id ?? 'guest'}-${isDoctor ? 'doctor' : 'patient'}`}
      root={{ ref }}
      {...props}
    />
  );
}

const LauncherMain = React.forwardRef(LauncherMain_);
export default LauncherMain;
